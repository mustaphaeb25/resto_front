# Saffron House frontend — environment variables
# Copy to .env and fill in values. Do NOT commit .env.

# Base URL of the deployed backend API (no trailing slash)
VITE_API_URL=https://restohotel-production.up.railway.app/api
